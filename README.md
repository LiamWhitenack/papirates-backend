# papirates-backend

This README is intentionally left sparse because the frontend is where the documentation should live. to get started, install cargo and run this command to start with `rustup`:
```curl --proto '=https' --tlsv1.2 -sSf https://sh.rustup.rs | sh```